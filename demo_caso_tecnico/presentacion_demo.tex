% Presentación demo — Caso técnico Rappi (Módulos 1, 2 y 3)
% Estructura: Problema → Métrica → Diagnóstico (M1) → Insight → Acción → Limitaciones → Extensión → observabilidad → cierre.
% Compilar: pdflatex presentacion_demo.tex (dos veces para TOC)
\documentclass[aspectratio=169,spanish]{beamer}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[spanish]{babel}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,calc}

\usetheme{Madrid}
\usecolortheme{default}

\definecolor{rappiaccent}{HTML}{9B2222}
\setbeamercolor{structure}{fg=rappiaccent}
\setbeamercolor{palette primary}{fg=white,bg=rappiaccent}
\setbeamercolor{palette secondary}{fg=white,bg=rappiaccent!85!black}
\setbeamercolor{frametitle}{fg=white,bg=rappiaccent}
\setbeamercolor{title}{fg=white}
\setbeamercolor{subtitle}{fg=white!92}
\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{footline}{}

\newenvironment{piegrafica}{%
  \par\vspace{0.5em}%
  \noindent
  \begin{minipage}[t]{\linewidth}%
  \raggedright
  \setlength{\parindent}{0pt}%
  \setlength{\parskip}{0.42em plus 0.12em minus 0.06em}%
  \scriptsize
}{%
  \end{minipage}\par
}

\newenvironment{piegraficafoot}{%
  \par\vspace{0.5em}%
  \noindent
  \begin{minipage}[t]{\linewidth}%
  \raggedright
  \setlength{\parindent}{0pt}%
  \setlength{\parskip}{0.42em plus 0.12em minus 0.06em}%
  \footnotesize
}{%
  \end{minipage}\par
}

\newcommand{\pizoneread}{%
  \begin{piegrafica}
  Frecuencia por hora del \texttt{ratio} en \emph{esta} zona (histórico del panel).

  Complementa los gráficos P1 agregados y muestra en qué franjas aparecen colas o picos.
  \end{piegrafica}%
}

\title[Sistema de alertas]{Alertas cuando llueve y la operación se aprieta}
\subtitle{Caso Rappi · Datos + reglas por zona + mensaje en Telegram}
\author{Demo técnica}
\date{\today}

\begin{document}

\begin{frame}
  \titlepage
\end{frame}

\begin{frame}{Qué vamos a ver}
  \tableofcontents
\end{frame}

% ---------------------------------------------------------------------------
\section{Problema}

\begin{frame}{Rappi pierde eficiencia por desbalance dinámico}
  \begin{itemize}
    \item La \textbf{oferta} (repartidores conectados) y la \textbf{demanda} (pedidos) \emph{no} se equilibran de forma estable: cambian por \textbf{zona}, \textbf{hora} y \textbf{contexto} (clima, día, incentivos).
    \item Ese desbalance se manifiesta en colas, saturación puntual y decisiones tarde: hacen falta \textbf{avisos a tiempo} y \textbf{números concretos} (zona, gravedad, cuánto subir incentivos en pesos).
    \item La solución del caso no es un ``modelo mágico'' a ciegas: primero se \textbf{aprende del histórico} (Excel + notebook) y luego el código aplica \textbf{reglas claras} y parámetros versionados (\texttt{calibration.json}).
  \end{itemize}
\end{frame}

\begin{frame}{Tres bloques, una idea cada uno}
  \begin{description}
    \item[Módulo 1] \textbf{Diagnóstico:} con el Excel y el notebook se entiende cómo reacciona cada zona; de ahí salen sensibilidades y umbrales.
    \item[Módulo 2] \textbf{Motor:} mira el \textbf{clima en vivo} (Open-Meteo), aplica las reglas por zona y dice riesgo e incentivos.
    \item[Módulo 3] \textbf{Mensaje:} convierte ese resultado en \textbf{texto para humanos} (modelo de lenguaje o plantilla de respaldo) y lo manda a \textbf{Telegram}.
  \end{description}
\end{frame}

\begin{frame}{Arquitectura del pipeline}
  \centering
  \includegraphics[width=\linewidth,height=0.80\textheight,keepaspectratio]{figures/pipeline_arquitectura.png}

  \vspace{0.45em}
  \small
  El M3 usa el \textbf{mismo motor} que el M2; solo añade redacción y el canal de aviso (Telegram).
\end{frame}

% ---------------------------------------------------------------------------
\section{Métrica clave: ratio pedidos/repartidor}

\begin{frame}{Qué mide el \texttt{ratio} y cómo se etiqueta el panel}
  \begin{itemize}
    \item \textbf{Definición:} \texttt{ratio} $=$ pedidos entre repartidores conectados en esa celda (zona $\times$ hora); resume el \textbf{apretón} operativo relativo.
    \item \textbf{Saturación (caso):} si el \texttt{ratio} supera \textbf{1{,}8} la celda se trata como saturada en el diagnóstico.
    \item \textbf{Otras bandas útiles:} sobre-oferta si ${<}\,0{,}5$; ``saludable'' entre $0{,}9$ y $1{,}2$; el resto es intermedio (misma convención que el notebook M1).
    \item \textbf{Panel:} 30 días $\times$ 24 h $\times$ 14 zonas $=$ 10\,080 filas en el Excel de referencia.
  \end{itemize}
\end{frame}

% ---------------------------------------------------------------------------
\section{Diagnóstico: zonas y horas críticas}

\begin{frame}{Módulo 1: para qué sirve}
  \begin{itemize}
    \item \textbf{Entrada:} el Excel del caso (\texttt{data/rappi\_delivery\_case\_data.xlsx}): pedidos, pagos, lluvia, hora, zona, si la zona iba ``saturada'', etc.
    \item \textbf{Herramienta:} notebook \texttt{01\_diagnostico\_operacional.ipynb}.
    \item \textbf{Salida humana:} ver cómo se mueve el \textbf{ratio} (pedidos por repartidor), cuándo hay saturación y cómo se relaciona con la lluvia \textbf{en cada zona}.
    \item \textbf{Salida para el código:} números que terminan en \texttt{calibration.json} (export desde script o lógica alineada al notebook).
  \end{itemize}
\end{frame}

\begin{frame}{Qué se calcula por zona (nombres técnicos)}
  \small
  \begin{itemize}
    \item \textbf{\texttt{precip\_coef}} — cuánto sube el ``apretón'' operativo cuando sube un poco la lluvia (estimado como en el notebook).
    \item \textbf{\texttt{sensitivity\_index}} — qué tan ``nerviosa'' es la zona frente a lluvia, comparada con el resto (número entre 0 y 1).
    \item \textbf{\texttt{alert\_precip\_mm\_hr}} — a partir de cuánta lluvia (mm/h) tiene sentido considerar alerta, según el histórico de horas ``malas''.
    \item \textbf{\texttt{mm\_precip\_healthy\_to\_saturation\_linear}} — orden de magnitud de lluvia para pasar de ``normal'' a ``muy cargado'' en esa zona.
    \item \textbf{\texttt{base\_earnings\_mxn}} — mediana histórica de pagos en la zona.
    \item \textbf{\texttt{recommended\_earnings\_mxn}} — media cuando ya llovía por encima del umbral (M1); el motor interpola entre base y recomendado según el ratio proyectado (tope global 35\,\%).
  \end{itemize}
\end{frame}

\begin{frame}{Cinco cosas que el diagnóstico deja claras}
  \footnotesize
  \begin{enumerate}
    \item El panel tiene \textbf{30 días $\times$ 24 h $\times$ 14 zonas} (10\,080 filas). El \textbf{ratio} es pedidos entre repartidores conectados; ``saturado'' si el ratio pasa de \textbf{1{,}8}.
    \item La lluvia \textbf{sí se asocia} con más presión operativa (modelo con hora y zona; el notebook muestra un $R^2$ alto en ese ajuste).
    \item Si en una hora hay más de \textbf{0{,}5 mm/h} de lluvia, la probabilidad de saturación es unas \textbf{8 veces} mayor que con lluvia muy suave o nula.
    \item \textbf{Cada zona es distinta:} por ejemplo Santiago reacciona antes que Mitras; los números en \texttt{calibration.json} lo reflejan.
    \item \textbf{Mapas:} la mayoría de polígonos vienen bien; donde falta, se usan centroides. Los umbrales de lluvia van \textbf{zona por zona}.
  \end{enumerate}
  \vspace{0.25em}
  \tiny Más detalle: \texttt{modulo1\_diagnostico/HALLAZGOS\_M1.md}
\end{frame}

\begin{frame}{Qué mirar en el diagnóstico (zonas y horas)}
  \small
  \begin{itemize}
    \item \textbf{Horas críticas:} en el notebook, P1 y tablas de $p_{\text{sat}}$ por \texttt{HOUR} muestran dónde se concentra la saturación histórica (p.\,ej.\ comida).
    \item \textbf{Zonas críticas:} ranking por frecuencia de saturación, \textit{risk score} y sensibilidad a lluvia difieren por territorio (Santiago vs Mitras, etc.).
    \item \textbf{Granularidad:} el análisis es \textbf{zona $\times$ hora}; agregar solo a día o solo a ciudad mezcla contextos y puede confundir lectura.
  \end{itemize}
\end{frame}

\input{presentacion_diagnostico_m1.tex}

\begin{frame}{Documentos y figuras}
  \begin{itemize}
    \item Informe en \texttt{modulo1\_diagnostico/Diagnóstico Operacional/}.
    \item Gráficos del notebook en \texttt{modulo1\_diagnostico/figures/}.
    \item Script \texttt{export\_calibration\_from\_m1.py} regenera \texttt{calibration.json} sin abrir Jupyter.
  \end{itemize}
\end{frame}

\begin{frame}{Del diagnóstico al motor}
  \centering
  \begin{tikzpicture}
    \node[draw, rounded corners, minimum width=8cm, minimum height=2.2cm, align=left, font=\small] {
      \textbf{Puente M1 $\rightarrow$ M2}\\[0.3em]
      Excel + notebook $\Rightarrow$ números por zona (cuánta lluvia dispara alerta, sensibilidad, pagos)\\[0.2em]
      $\Downarrow$\\[0.2em]
      \texttt{calibration.json}
    };
  \end{tikzpicture}
\end{frame}

% ---------------------------------------------------------------------------
\section{Insight clave: no es solo demanda}

\begin{frame}{Síntesis exploratoria (panel por zonas)}
  \small
  \textbf{Lectura exploratoria (Excel + notebook)}\\[0.6em]
  \begin{itemize}
    \item \textbf{Al parecer}, las zonas con perfil más \textbf{rural o periférico} concentran con mayor frecuencia \textbf{anomalías tipo saturación}: distribuciones más anchas y picos extremos (ratio muy alto en ciertas horas), coherentes con episodios de mucha demanda relativa frente a reparto disponible.
    \item Las zonas \textbf{urbanas y residenciales} tienden, en cambio, a que su \textbf{valor máximo habitual} quede \textbf{dentro del rango esperado} antes del umbral de saturación del panel: menos cola hacia valores extremos en el histórico mostrado.
    \item \textbf{Matiz:} conviene validar con el notebook y con la definición operativa de cada zona (no sustituye un estudio urbano formal).
  \end{itemize}
\end{frame}

\begin{frame}{Es contexto + tiempo}
  \begin{itemize}
    \item El estrés operativo no se reduce a ``hubo muchos pedidos'': importan \textbf{cuándo} (franja, festivo), \textbf{dónde} (zona) y \textbf{qué pasaba afuera} (lluvia) y con \textbf{qué incentivos} se operaba.
    \item El \texttt{ratio} resume desbalance oferta--demanda, pero su interpretación válida es \textbf{condicional} a hora y territorio.
    \item Por eso el caso calibra \textbf{por zona} y el motor mira \textbf{ventanas cortas} de clima, en lugar de una sola regla global.
  \end{itemize}
\end{frame}

% ---------------------------------------------------------------------------
\section{Acción: sistema de alertas y umbrales operativos}

\begin{frame}{Clima en vivo}
  \begin{itemize}
    \item \textbf{Open-Meteo:} pronóstico horario de lluvia, \textbf{sin API key} en el caso.
    \item Las unidades encajan con el Excel (mm por hora como intensidad).
    \item Zona horaria: \texttt{America/Monterrey}.
    \item Por defecto se miran unas \textbf{2 horas} hacia adelante (balance entre precisión y tiempo para reaccionar).
  \end{itemize}
\end{frame}

\begin{frame}{Mapas: ¿de dónde sale el punto del tiempo?}
  \begin{itemize}
    \item Cada zona tiene un polígono en el Excel; se usa geometría (\textbf{Shapely}) para elegir un punto \textbf{dentro} del polígono cuando el mapa es válido.
    \item Si el polígono viene mal, se usan las coordenadas de respaldo de \texttt{ZONE\_INFO}.
    \item Opcional: más puntos por rejilla para escenarios finos (\texttt{geo\_pipeline}).
  \end{itemize}
\end{frame}

\begin{frame}{Qué hace el motor (pasos sencillos)}
  \small
  \begin{enumerate}
    \item Por zona: mira la lluvia máxima esperada frente al \textbf{umbral de esa zona}.
    \item Elige la zona \textbf{más preocupante} (mayor exceso sobre el umbral).
    \item Calcula cómo podría moverse el \textbf{ratio} con la lluvia prevista.
    \item Asigna \textbf{riesgo:} BAJO, MEDIO, ALTO o CRÍTICO.
    \item Propone \textbf{cuánto subir incentivos} y qué otras zonas vigilar.
  \end{enumerate}
\end{frame}

\begin{frame}{Para no ahogar al coordinador (debounce)}
  \begin{itemize}
    \item Se guarda un \textbf{estado por zona} (archivo local).
    \item No se repite el \textbf{mismo tipo de aviso} en poco tiempo (por defecto unos \textbf{45 minutos}), salvo que la situación \textbf{empeore de verdad} o suba mucho el \textbf{nivel de riesgo}.
    \item Ese tiempo se puede cambiar con \texttt{ALERT\_DEBOUNCE\_TTL\_SEC} en \texttt{.env}.
    \item \textbf{Extra:} deduplicación del mismo ``evento'' de lluvia y, si se configura, tiempo mínimo entre alertas de \textbf{cualquier} zona (\texttt{ALERT\_GLOBAL\_MIN\_INTERVAL\_SEC}).
  \end{itemize}
\end{frame}

\begin{frame}{Umbrales operativos: \texttt{ratio} y lluvia por zona}
  \small
  \begin{itemize}
    \item \textbf{\texttt{ratio}:} saturación del caso a \texttt{ratio} ${>} 1{,}8$; el motor proyecta cómo podría moverse el \texttt{ratio} con la lluvia esperada y asigna \textbf{riesgo} (BAJO $\rightarrow$ CRÍTICO).
    \item \textbf{Lluvia (\texttt{alert\_precip\_mm\_hr}):} se calcula con \texttt{export\_calibration\_from\_m1.py}; en horas ya saturadas en histórico, percentil 75 de mm/h por zona.
    \item \textbf{Respaldo 6{,}55 mm/h} si el percentil sale casi cero (muchas saturaciones ``en seco'') para no alertar por ruido.
    \item Reglas en lenguaje llano: \texttt{motor\_reglas/motor\_reglas.tex} $\rightarrow$ PDF.
  \end{itemize}
\end{frame}

\begin{frame}{Cómo probar el motor solo}
  \texttt{modulo2\_motor\_alertas/run\_alert\_engine.py}
  \vspace{0.5em}
  \begin{itemize}
    \item \texttt{python run\_alert\_engine.py} — usa clima real, resultado en consola.
    \item \texttt{python run\_alert\_engine.py --demo} — simula lluvia fuerte en Santiago sin internet.
    \item \texttt{python run\_alert\_engine.py --recalibrate} — vuelve a armar \texttt{calibration.json} desde el Excel y ejecuta.
  \end{itemize}
  \vspace{0.3em}
  \small Reglas explicadas en lenguaje llano: \texttt{motor\_reglas/motor\_reglas.tex} $\rightarrow$ PDF.
\end{frame}

\begin{frame}{Módulo 3 — Telegram: mismo motor, texto para humanos}
  \begin{itemize}
    \item Mismo clima y \textbf{mismo motor} que el M2.
    \item El programa arma un \textbf{resumen en JSON} (zona, riesgo, números); el modelo de lenguaje solo \textbf{redacta} el mensaje. No “busca en Google”: el contenido viene del motor.
    \item Puede usarse \textbf{Ollama} en local o \textbf{OpenAI}; con OpenAI, por defecto entra \textbf{LangChain}. Si el modelo falla, hay \textbf{texto fijo} con los mismos datos.
    \item El envío es con \textbf{python-telegram-bot}.
    \item Resumen en PDF (lenguaje llano): \texttt{modulo3\_agente\_telegram/latex/arquitectura\_agente.tex}.
  \end{itemize}
\end{frame}

\begin{frame}{Qué debe verse en el mensaje (unos 10 segundos)}
  \begin{itemize}
    \item Zona y \textbf{qué tan grave} es.
    \item Qué lluvia se espera y en qué ventana.
    \item Una frase de \textbf{contexto histórico} (guiada por el sistema).
    \item \textbf{Acción clara:} subir pagos de X a Y pesos en Z minutos.
    \item Otras \textbf{zonas a vigilar}.
  \end{itemize}
\end{frame}

\begin{frame}{Configuración y cuidado con el token}
  \begin{itemize}
    \item En \texttt{caso\_tecnico/.env}: token del bot y chat/canal (\texttt{TELEGRAM\_*}).
    \item El bot debe poder \textbf{publicar} en el canal.
    \item \textbf{No subir el token al repositorio.} Si se filtra, revocar en BotFather.
  \end{itemize}
\end{frame}

\begin{frame}{Comandos útiles en demo}
  \small
  Desde la raíz \texttt{caso\_tecnico/}:
  \vspace{0.4em}
  \begin{block}{Pruebas}
    \texttt{python modulo3\_agente\_telegram/run\_agent.py --test-telegram}\\[0.2em]
    \texttt{python modulo3\_agente\_telegram/run\_agent.py --demo --dry-run}\\[0.2em]
    \texttt{python modulo3\_agente\_telegram/run\_agent.py --demo --validate}
  \end{block}
  \begin{block}{Enviar de verdad al canal}
    \texttt{python modulo3\_agente\_telegram/run\_agent.py --demo --force-send}
  \end{block}
\end{frame}

\begin{frame}{Extras opcionales}
  \begin{itemize}
    \item Si el motor \textbf{silencia} un aviso por debounce, puede mandarse un \textbf{aviso corto} (salvo prueba en seco con \texttt{--dry-run}).
    \item Queda registro de envíos en archivos locales (\texttt{.alert\_events.jsonl}).
    \item \textbf{Resumen del día:} lista de eventos y comparación con la lluvia registrada; se puede programar con \textbf{cron} (ver README del módulo 3).
  \end{itemize}
\end{frame}

\begin{frame}{Monitor que se ejecuta solo}
  \small
  \begin{itemize}
    \item Script \texttt{monitor\_loop.py}: cada cierto tiempo vuelve a consultar el clima, pasa por el motor y, si toca, prepara Telegram.
    \item Por defecto el intervalo es \textbf{10 minutos} (configurable; no confundir con el tiempo entre avisos repetidos, que es el debounce).
    \item Los ciclos se guardan en archivos que también lee el \textbf{dashboard web} (\texttt{/monitor/}).
  \end{itemize}
  \vspace{0.25em}
  \footnotesize
  Prueba rápida: \texttt{python modulo3\_agente\_telegram/monitor\_loop.py --once --demo --dry-run}
\end{frame}

\begin{frame}{Dos tiempos distintos (no mezclar)}
  \begin{itemize}
    \item \textbf{Cada cuánto se mira el clima} (monitor): p.\,ej. cada 10 min.
    \item \textbf{Cada cuánto se puede repetir} un aviso parecido en la misma zona (debounce): p.\,ej. $\sim$45 min.
    \item Si el \textbf{riesgo sube de nivel}, puede avisarse antes aunque no haya pasado el tiempo largo.
  \end{itemize}
\end{frame}

% ---------------------------------------------------------------------------
\section{Limitaciones}

\begin{frame}{Qué \emph{no} afirma este caso}
  \small
  \begin{itemize}
    \item \textbf{No hay causalidad pura:} regresiones y correlaciones en un panel fijo describen \textbf{asociaciones} condicionadas; no sustituyen A/B tests, instrumentos ni datos de operación en tiempo real completos.
    \item \textbf{Sin NPS ni experiencia cliente:} no entran cancelaciones, tiempos por etapa, ni satisfacción; conviene cruzar con esos KPIs antes de decisiones fuertes de producto o pricing.
    \item \textbf{Extracto y ciudad acotados:} un mes y 14 zonas; recalibrar al cambiar mercado, estación o definición de polígonos.
    \item \textbf{Clima y pronóstico:} Open-Meteo puede equivocarse; los umbrales y el debounce mitigan ruido pero no eliminan incertidumbre meteorológica.
  \end{itemize}
\end{frame}

% ---------------------------------------------------------------------------
\section{Extensión futura}

\begin{frame}{Próximos pasos posibles}
  \begin{itemize}
    \item \textbf{ML predictivo:} modelo de \texttt{ratio} o de probabilidad de saturación en ventanas cortas (features: lluvia prevista, hora, zona, histórico reciente), en paralelo a reglas interpretables.
    \item \textbf{Optimización logística:} acoplar sugerencias de incentivos con restricciones reales de flota, costo marginal y políticas comerciales; cerrar el ciclo con experimentación controlada.
    \item \textbf{Más señales:} demanda prevista, eventos locales, integración con despacho en vivo.
  \end{itemize}
\end{frame}

% ---------------------------------------------------------------------------
\section{Observabilidad y auditoría}

\begin{frame}{Trazas locales (auditoría)}
  \small
  \begin{itemize}
    \item \textbf{\texttt{.ops\_audit.jsonl}} (M2): decisiones del motor, debounce, fallos de clima — útil para depurar.
    \item \textbf{\texttt{.alert\_events.jsonl}}: una línea por cada alerta \textbf{enviada} a Telegram (no dry-run).
    \item \textbf{\texttt{.monitor\_ticks.jsonl}} / \textbf{\texttt{.monitor\_status.json}}: ciclos del \texttt{monitor\_loop} para el panel web.
    \item No van al git; viven bajo \texttt{modulo2\_motor\_alertas/}.
  \end{itemize}
\end{frame}

% ---------------------------------------------------------------------------
\section{Cierre}

\begin{frame}{Mitigaciones operativas (demo y producción)}
  \small
  \begin{itemize}
    \item \textbf{Clima errático:} umbrales por zona + criterio humano; debounce reduce spam sin bloquear escaladas de riesgo.
    \item \textbf{Demasiados mensajes:} avisos cortos cuando el motor silencia por TTL; subida real de riesgo puede pasar antes.
    \item \textbf{Otra ciudad o periodo:} nuevo Excel, nueva calibración, mismo código y documentación de reglas.
  \end{itemize}
  \vspace{0.3em}
  \footnotesize
  Guías: \texttt{docs/criterios\_evaluacion\_demo.md}, \texttt{modulo3\_agente\_telegram/docs/qa\_arquitectura.md}.
\end{frame}

\begin{frame}{}
  \centering
  {\Huge Gracias}

  \vspace{1.2em}
  {\large Preguntas}
\end{frame}

\end{document}
